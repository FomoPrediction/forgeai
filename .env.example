# Where the dashboard app (Robotics/app) is served.
# Dev: the app runs on 3001, this landing page on 3000.
# Prod: set this to the deployed app URL before `next build` — NEXT_PUBLIC_*
# values are inlined at build time, not read at runtime.
NEXT_PUBLIC_APP_URL=http://localhost:3001
